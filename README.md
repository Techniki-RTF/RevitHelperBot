# RevitHelperBot — база знаний и консультант по работе с Revit

## Функционал

### Основные возможности
- **База знаний**: минималистичная база знаний с возможностью добавления статей прямо в Telegram и поддержкой таблиц
- **Пошаговая консультация**: бот проходит 8 ключевых шагов и фиксирует ответы (сделано/пропустить), в конце показывает **итоги консультации**.

### Чеклист консультации (8 шагов)
1. **Включить Акселератор**: ускоряет открытие файла из другого офиса на 40–60%.
2. **Проверить свободное место на диске C**: нехватка места сильно замедляет Windows и Revit.
3. **Закрыть лишние вкладки/браузеры**: вкладки занимают ОЗУ — её активно использует Revit.
4. **Проверить Рабочие Наборы (РН)**: элементы должны принадлежать своим РН; минимум — РН для связанных файлов.
5. **Проверить неиспользуемые объекты**: группы, DWG, виды увеличивают вес и фоновые обновления.
6. **Контроль веса файла хранилища**: ориентир 300–500 МБ; при необходимости — пересохранение со сжатием.
7. **Аналитические данные модели**: фоновые расчёты могут замедлять; отключать по согласованию с проектировщиком/руководителем.
8. **Семейства**: использовать корпоративную библиотеку; сторонние отдавать BIM‑координатору для оптимизации.

### База знаний
- **Просмотр статей**: раздел **«База знаний»** открывает список сохранённых статей с быстрым переходом к содержимому.
- **Добавление статей (только для админов)**: админ по кнопке **«Добавить статью»** проходит два шага — вводит название и текст статьи, затем подтверждает добавление.
- **Редактирование через перезапись**: если сохранить статью с уже существующим названием, её содержимое будет **перезаписано**.
- **Удаление статей**: под открытой статьёй есть кнопка **«Удалить»**, которая убирает её из базы.
- **Текстовые таблицы в статьях**: внутри статьи можно описывать таблицы в текстовом виде, и бот отрисует их аккуратным моноширинным блоком.

#### Синтаксис таблиц в статьях
- **Обёртка таблицы**:
  - начало: `[table:Название]` или просто `[table]` (без названия);
  - конец: `[/table]` на отдельной строке.
- **Строки таблицы**:
  - каждая строка внутри блока — отдельная строка таблицы;
  - колонки разделяются символом `;`.
- **Пример**:

Разметка:
```text
[table:Размеры колонн]
Тип;B;H
Колонна 300x400;300;400
Колонна 400x400;400;400
[/table]
```
Результат:
```text
📎 Таблица: Размеры колонн
Тип              | B   | H
-----------------+-----+---
Колонна 300x400  | 300 | 400
Колонна 400x400  | 400 | 400
```

### Роли и доступ
- **Администраторы**:
  - могут добавлять и удалять статьи в базе знаний;
  - список админов настраивается через переменную окружения `ADMINS` (список Telegram ID через запятую).
- **Обычные пользователи**:
  - могут проходить консультацию;
  - могут просматривать статьи и таблицы, но не редактировать базу.

## Установка и запуск

### Создайте файл `.env` в корне проекта со значениями окружения:

```dotenv
TOKEN=123456:ABCDEF_your_telegram_bot_token # from t.me/BotFather
ADMINS=123456789 # telegram ids

DB_USER=admin
DB_PASS=password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=revitbot_db

PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
```
### Запустите бота через Docker Compose

```bash
docker compose up --build
```